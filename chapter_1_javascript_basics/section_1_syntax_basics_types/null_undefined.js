console.log("\nTOPIC: NULL & UNDEFINED TYPES")

let age;    // automatically set to undefined
console.log(age, age + 3, `age is ${age}`)

let size = null // explicitly set to null
console.log(size, size + 3, `size is ${size}`)